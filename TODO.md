# TODO — Add client images (without breaking responsiveness)

- [x] Update `index.html`: add `assets/heroimg.png` in hero visual area
- [x] Update `about.html`: replace visual image with `assets/Aboutimg.png`
- [x] Update `services.html`: update each `.sd-visual` to use corresponding `assets/*.png` (background/visual) while keeping existing layout/aspect-ratio
- [x] Update `styles.css` ONLY if needed for image fit (aspect-ratio/object-fit) using new lightweight classes (no breakpoint changes)
- [ ] Quick sanity check: ensure all image paths correct and pages render (desktop + mobile)


